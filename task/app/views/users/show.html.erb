<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Country:</strong>
  <%= @user.country.name %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<!-- replace up line with
<strong>Country:</strong>
<% if @user.country.name? %>
<%= @user.country.name %>
<% end %>

or and this is best
<%= @user.try(:country).try(:name) %> -->
